export default function PlacementChoices({label,name,options,values,onChange}:{label:string;name:string;options:{value:string;label:string}[];values:string[];onChange:(values:string[])=>void}) {
 return <fieldset className="min-w-0"><legend className="text-sm font-medium">{label}</legend><details className="relative mt-1.5 rounded-xl border border-slate-300 p-3 dark:border-slate-700"><summary className="cursor-pointer text-sm">{values.length?`${values.length} selected`:`Select ${label.toLowerCase()}`}</summary><div className="mt-3 max-h-48 space-y-2 overflow-y-auto">{options.length?options.map(o=><label key={o.value} className="flex items-center gap-2 text-sm"><input type="checkbox" name={name} value={o.value} checked={values.includes(o.value)} onChange={e=>onChange(e.target.checked?[...values,o.value]:values.filter(v=>v!==o.value))}/>{o.label}</label>):<p className="text-sm text-slate-500">No options available. Add the relevant academic details and students first.</p>}</div></details></fieldset>;
}
